package net.scottpullen.handlers;

import ratpack.func.Action;
import ratpack.handling.Chain;

public class SessionChain implements Action<Chain> {
    @Override
    public void execute(Chain chain) throws Exception {

    }
}
